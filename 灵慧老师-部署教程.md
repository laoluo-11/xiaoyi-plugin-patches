# 灵慧老师 — OpenClaw 多用户部署完整教程

> 版本：V3
> 日期：2026-07-18
> 适用：OpenClaw v2026.6.11+

---

## 零、前提条件

- Ubuntu/Debian 服务器，能访问外网（小艺通过 WebSocket 直连华为云，无需公网 IP 或域名）
- Node.js 22+（`node --version`）
- 小艺开放平台已注册 Agent，获取 AK/SK 和 agentId

---

## 一、安装 OpenClaw

```bash
# 安装
curl -fsSL --proto '=https' --tlsv1.2 https://openclaw.ai/install.sh | bash

# 中文界面
export OPENCLAW_LOCALE=zh-CN
echo 'export OPENCLAW_LOCALE=zh-CN' >> ~/.bashrc

# 验证
openclaw --version
```

---

## 二、配置 Provider

### 2.1 DeepSeek（主模型，文字对话）

```bash
openclaw config set models.providers.deepseek.apiKey "sk-your-deepseek-key"
openclaw config set models.providers.deepseek.baseUrl "https://api.deepseek.com"
openclaw config set models.providers.deepseek.api "openai-completions"
```

### 2.2 OpenRouter（图片识别，Qwen VL）

DeepSeek 不支持图片，需要配一个多模态模型做 vision fallback。

> 注意：Google/OpenAI/Anthropic 模型在 OpenRouter 上对中国 IP 通常报 403 region-blocked。
> Qwen VL（阿里）和 Llama 可用。推荐 Qwen2.5-VL-72B。

```bash
# 1. 去 https://openrouter.ai/keys 创建 API key

# 2. 配置
openclaw config set models.providers.openrouter.apiKey "sk-or-v1-xxxx"
openclaw config set models.providers.openrouter.baseUrl "https://openrouter.ai/api/v1"
openclaw config set models.providers.openrouter.api "openai-completions"

# 3. 设置图片模型（主模型不支持 vision 时自动路由到这里）
openclaw config set agents.defaults.imageModel "openrouter/qwen/qwen2.5-vl-72b-instruct"
```

### 2.3 模型配置

```bash
openclaw config set agents.defaults.model.primary "deepseek/deepseek-v4-flash"
```

---

## 三、配置会话和多用户

```bash
# 每个 channel peer 独立 session
openclaw config set session.dmScope "per-channel-peer"

# 并发数（默认 4，多设备场景建议 20+）
openclaw config set agents.defaults.maxConcurrent 20
```

---

## 四、安装小艺 Channel 插件

在 OpenClaw 控制面板或命令行安装 `@ynhcj/xiaoyi` 插件，配置 AK/SK（从小艺开放平台获取）：

```bash
openclaw config set channels.xiaoyi.enabled true
openclaw config set channels.xiaoyi.ak "your-access-key"
openclaw config set channels.xiaoyi.sk "your-secret-key"
openclaw config set channels.xiaoyi.agentId "agent7baa..."
```

---

## 五、打补丁 — 多用户隔离 + 图片隔离

插件安装在 npm 目录，需要手动修改 3 个文件。**每次 `openclaw plugins update` 后需重新打补丁。**

### 5.1 找到插件目录

```bash
PLUGIN_DIR=$(ls -d ~/.openclaw/npm/projects/ynhcj-xiaoyi-*/node_modules/@ynhcj/xiaoyi/dist/ | head -1)
echo $PLUGIN_DIR
```

### 5.2 补丁 1：xy-parser.js — 提取 deviceId

找到 `parseA2AMessage` 函数的 return 语句，确认包含：

```javascript
return {
    sessionId,
    taskId: paramsId,
    messageId: id,
    parts: message.parts || [],
    deviceId: request.deviceId,        // ← 这一行
    deviceType: request.deviceType,    // ← 这一行
    method,
};
```

### 5.3 补丁 2：xy-bot.js — session 路由 + 消息注入 + 图片设备隔离

**a) 顶部添加 imports：**

```javascript
const fs = require("fs");
const path = require("path");
const os = require("os");
```

**b) 修改 peer.id（在 `resolveAgentRoute` 调用处）：**

```javascript
peer: {
    kind: "direct",
    id: parsed.deviceId || parsed.sessionId,  // deviceId 优先
},
```

**c) 在 `downloadFilesFromParts` 调用处传入 deviceId：**

```javascript
const mediaList = await (0, file_download_js_1.downloadFilesFromParts)(
    fileParts, undefined, parsed.deviceId || parsed.sessionId
);
```

**d) 在 `extractTextFromParts` 之后、`messageBody` 之前插入设备上下文注入：**

```javascript
// Device-scoped memory context
const deviceId = parsed.deviceId || parsed.sessionId;
const workspaceRoot = (cfg && cfg.agents && cfg.agents.defaults && cfg.agents.defaults.workspace)
    || path.join(os.homedir(), '.openclaw', 'workspace');
const deviceDir = path.join(workspaceRoot, 'devices', deviceId);
const deviceUserPath = path.join(deviceDir, 'USER.md');
const deviceMemoryPath = path.join(deviceDir, 'MEMORY.md');

if (!fs.existsSync(deviceDir)) {
    fs.mkdirSync(deviceDir, { recursive: true });
    const userTemplate = '# USER.md - 本设备专属用户档案\n\n> 仅供当前设备（' + deviceId + '）使用。\n\n- **Name:** （待了解）\n- **What to call them:** （待了解）\n- **Notes:** \n';
    const memoryTemplate = '# MEMORY.md - 本设备专属任务记录\n\n> 仅供当前设备使用。\n\n## 任务列表\n\n（暂无）\n';
    fs.writeFileSync(deviceUserPath, userTemplate);
    fs.writeFileSync(deviceMemoryPath, memoryTemplate);
}

let deviceUserMd = '';
let deviceMemoryMd = '';
try { deviceUserMd = fs.readFileSync(deviceUserPath, 'utf-8'); } catch(e) {}
try { deviceMemoryMd = fs.readFileSync(deviceMemoryPath, 'utf-8'); } catch(e) {}

const devicePrefix = '[系统] 当前设备ID: ' + deviceId + '\n'
    + '本设备专属目录（绝对路径）: ' + deviceDir + '\n'
    + '用户档案: ' + deviceUserPath + '\n'
    + '任务记录: ' + deviceMemoryPath + '\n'
    + '安全规则: 只读写上述绝对路径下的文件。严禁访问工作空间根目录的 USER.md/MEMORY.md（已设只读保护）。严禁访问其他设备目录。\n\n'
    + '## 本设备用户档案\n' + deviceUserMd + '\n'
    + '## 本设备任务记录\n' + deviceMemoryMd + '\n'
    + '---以下是用户消息---\n';

let messageBody = devicePrefix + (text || "");
```

### 5.4 补丁 3：file-download.js — 图片按设备分目录

修改 `downloadFilesFromParts` 函数签名和开头的目录处理：

```javascript
async function downloadFilesFromParts(fileParts, tempDir = "...", deviceId = "default") {
    // Append device ID for per-device isolation
    tempDir = path.join(tempDir, deviceId);
    // Create temp directory if it doesn't exist
    await fs.promises.mkdir(tempDir, { recursive: true });
    // ... rest unchanged
```

---

## 六、配置 SOUL.md

编辑 `~/.openclaw/workspace/SOUL.md`，末尾添加：

```markdown
## 记忆管理（硬性规则）

**每个设备有独立的文件目录，系统已自动注入绝对路径到每条消息中。**

1. 你的专属目录在消息开头已标明绝对路径 — 你只能读写此目录下的文件
2. 工作空间根目录的 USER.md 和 MEMORY.md 已设置只读保护 — 你无法写入，也不需要读取
3. 用户档案 — 记录在消息开头标明的绝对路径下的 USER.md 中
4. 任务记录 — 记录在消息开头标明的绝对路径下的 MEMORY.md 中
5. 禁止跨越设备 — 每个设备的目录名就是设备 ID
```

---

## 七、工作空间初始化

```bash
# 创建设备目录
mkdir -p ~/.openclaw/workspace/devices

# 创建图片目录
mkdir -p ~/.openclaw/workspace/media/xy_channel

# 全局文件设为只读（硬防护）
chmod 444 ~/.openclaw/workspace/USER.md
chmod 444 ~/.openclaw/workspace/MEMORY.md

# 归档旧记忆（如有）
mkdir -p ~/.openclaw/workspace/memory-archive
mv ~/.openclaw/workspace/memory/*.md ~/.openclaw/workspace/memory-archive/ 2>/dev/null
```

---

## 八、备份补丁

```bash
mkdir -p ~/xiaoyi-plugin-patches
PLUGIN_DIR=$(ls -d ~/.openclaw/npm/projects/ynhcj-xiaoyi-*/node_modules/@ynhcj/xiaoyi/dist/ | head -1)
cp $PLUGIN_DIR/xy-parser.js ~/xiaoyi-plugin-patches/
cp $PLUGIN_DIR/xy-bot.js ~/xiaoyi-plugin-patches/
cp $PLUGIN_DIR/file-download.js ~/xiaoyi-plugin-patches/
cp ~/.openclaw/workspace/SOUL.md ~/xiaoyi-plugin-patches/
```

---

## 九、重启生效

```bash
openclaw gateway restart
```

---

## 十、验证

1. 发送文字 → DeepSeek 正常回复 ✓
2. 发送图片 → Agent 能识别图片内容 ✓
3. 设备A说"我叫小明" → devices/<A>/USER.md 记录小明 ✓
4. 设备B问"我叫什么" → 回答设备B自己的名字，不是小明 ✓
5. 设备A发图 → media/xy_channel/<A>/ 下有图 ✓
6. 设备B发图 → media/xy_channel/<B>/ 下有图，两个目录不混 ✓

---

## 十一、最终配置汇总

```bash
# 查看全部生效配置
openclaw config get agents.defaults.model.primary
openclaw config get agents.defaults.imageModel
openclaw config get agents.defaults.maxConcurrent
openclaw config get session.dmScope
openclaw config get models.providers.deepseek.apiKey
openclaw config get models.providers.openrouter.apiKey
openclaw config get channels.xiaoyi
```

---

## 十二、补丁恢复命令

插件更新后补丁会被覆盖，执行以下恢复：

```bash
PLUGIN_DIR=$(ls -d ~/.openclaw/npm/projects/ynhcj-xiaoyi-*/node_modules/@ynhcj/xiaoyi/dist/ | head -1)
cp ~/xiaoyi-plugin-patches/xy-parser.js $PLUGIN_DIR/
cp ~/xiaoyi-plugin-patches/xy-bot.js $PLUGIN_DIR/
cp ~/xiaoyi-plugin-patches/file-download.js $PLUGIN_DIR/
cp ~/xiaoyi-plugin-patches/SOUL.md ~/.openclaw/workspace/
openclaw gateway restart
```
